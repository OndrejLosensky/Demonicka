import SwiftUI

struct ParticipantsView: View {
    var body: some View {
        NavigationView {
            Text("Participants")
                .navigationTitle("Participants")
        }
    }
}

#Preview {
    ParticipantsView()
} 