import SwiftUI

struct EventsView: View {
    var body: some View {
        NavigationView {
            Text("Events")
                .navigationTitle("Events")
        }
    }
}

#Preview {
    EventsView()
} 