{
  "name": "demonicka-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "Démonická monorepo",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:server": "turbo run build --filter=demonicka-server",
    "build:web": "turbo run build --filter=demonicka-web",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.7.4",
    "typescript": "^5.7.3"
  }
}
